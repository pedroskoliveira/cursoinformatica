import { DayLesson } from '../types';

export const INITIAL_VIDEO_LESSONS: Record<number, any[]> = {};

export const applyInitialVideoLessons = (lesson: DayLesson): DayLesson => {
  return lesson;
};
